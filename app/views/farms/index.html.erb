<% content_for :title, "Farm Management" %>

<!-- Modern Header -->
<div class="farms-header mb-5">
  <div class="row align-items-center">
    <div class="col-lg-8">
      <div class="d-flex align-items-center">
        <div class="header-icon me-3">
          <i class="bi bi-house-door"></i>
        </div>
        <div>
          <h1 class="header-title mb-0">🏡 Farm Management</h1>
          <p class="header-subtitle mb-0">
            Manage all your dairy farms and their operations
          </p>
        </div>
      </div>
    </div>
    <div class="col-lg-4 text-end">
      <div class="header-actions">
        <%= link_to new_farm_path, class: "btn btn-success me-2" do %>
          <i class="bi bi-plus-circle me-2"></i>Add New Farm
        <% end %>
        <div class="btn-group">
          <button class="btn btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown">
            <i class="bi bi-three-dots"></i>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <li>
              <a class="dropdown-item" href="#" id="exportFarms">
                <i class="bi bi-download me-2"></i>Export Data
              </a>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <%= link_to root_path, class: "dropdown-item" do %>
                <i class="bi bi-house me-2"></i>Dashboard
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Farm Statistics Overview -->
<% if @farms.any? %>
  <div class="farm-stats-container mb-5">
    <div class="row g-4">
      <div class="col-md-3 col-sm-6">
        <div class="stat-card primary-stat">
          <div class="stat-content">
            <div class="stat-icon">
              <i class="bi bi-house-door"></i>
            </div>
            <div class="stat-info">
              <h3 class="stat-value"><%= @farms.count %></h3>
              <p class="stat-label">Total Farms</p>
              <small class="stat-trend positive">
                <i class="bi bi-arrow-up"></i>
                Active farms
              </small>
            </div>
          </div>
          <div class="stat-gradient primary-gradient"></div>
        </div>
      </div>
      
      <div class="col-md-3 col-sm-6">
        <div class="stat-card success-stat">
          <div class="stat-content">
            <div class="stat-icon">
              <i class="bi bi-emoji-smile"></i>
            </div>
            <div class="stat-info">
              <h3 class="stat-value"><%= @total_cows %></h3>
              <p class="stat-label">Total Cows</p>
              <small class="stat-trend positive">
                <i class="bi bi-heart-fill"></i>
                Healthy herd
              </small>
            </div>
          </div>
          <div class="stat-gradient success-gradient"></div>
        </div>
      </div>
      
      <div class="col-md-3 col-sm-6">
        <div class="stat-card info-stat">
          <div class="stat-content">
            <div class="stat-icon">
              <i class="bi bi-droplet-fill"></i>
            </div>
            <div class="stat-info">
              <h3 class="stat-value">
                <%= number_with_precision(@farms.sum { |f| f.today_production }, precision: 1) %>L
              </h3>
              <p class="stat-label">Today's Production</p>
              <small class="stat-trend positive">
                <i class="bi bi-calendar-day"></i>
                Daily yield
              </small>
            </div>
          </div>
          <div class="stat-gradient info-gradient"></div>
        </div>
      </div>
      
      <div class="col-md-3 col-sm-6">
        <div class="stat-card warning-stat">
          <div class="stat-content">
            <div class="stat-icon">
              <i class="bi bi-graph-up"></i>
            </div>
            <div class="stat-info">
              <h3 class="stat-value">
                <%= number_with_precision(@farms.any? ? @farms.sum { |f| f.today_production } / @farms.count : 0, precision: 1) %>L
              </h3>
              <p class="stat-label">Avg per Farm</p>
              <small class="stat-trend neutral">
                <i class="bi bi-bar-chart"></i>
                Performance
              </small>
            </div>
          </div>
          <div class="stat-gradient warning-gradient"></div>
        </div>
      </div>
    </div>
  </div>

  <!-- Search and Filter Section -->
  <div class="farms-filter-section mb-4">
    <div class="filter-card">
      <div class="row g-3 align-items-center">
        <div class="col-md-6">
          <div class="search-container">
            <i class="bi bi-search search-icon"></i>
            <input type="text" class="form-control search-input" id="farmSearch" 
                   placeholder="Search farms by name, owner, or location...">
          </div>
        </div>
        <div class="col-md-4">
          <select class="form-select" id="farmSort">
            <option value="name">Sort by Name</option>
            <option value="cows">Sort by Cow Count</option>
            <option value="production">Sort by Production</option>
            <option value="location">Sort by Location</option>
          </select>
        </div>
        <div class="col-md-2">
          <div class="view-options">
            <div class="btn-group w-100" role="group">
              <button type="button" class="btn btn-outline-secondary active" id="gridView">
                <i class="bi bi-grid"></i>
              </button>
              <button type="button" class="btn btn-outline-secondary" id="listView">
                <i class="bi bi-list"></i>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Grid View -->
  <div class="farms-container grid-view" id="gridViewContainer">
    <div class="row g-4" id="farmsGrid">
      <% @farms.each_with_index do |farm, index| %>
        <div class="col-xl-4 col-lg-6 col-md-6 farm-item" 
             data-name="<%= farm.name.downcase %>" 
             data-owner="<%= farm.owner.downcase %>" 
             data-location="<%= farm.location.downcase %>"
             data-cows="<%= farm.total_cows %>"
             data-production="<%= farm.today_production %>">
          <div class="farm-card" data-farm-id="<%= farm.id %>">
            <div class="farm-card-header">
              <div class="farm-header-content">
                <div class="farm-icon">
                  <i class="bi bi-house-door-fill"></i>
                </div>
                <div class="farm-basic-info">
                  <h5 class="farm-name"><%= farm.name %></h5>
                  <p class="farm-owner">
                    <i class="bi bi-person me-1"></i>
                    <%= farm.owner %>
                  </p>
                </div>
              </div>
              <div class="farm-status">
                <span class="status-badge active">
                  <i class="bi bi-circle-fill"></i>
                  Active
                </span>
              </div>
            </div>
            
            <div class="farm-card-body">
              <div class="farm-location mb-3">
                <i class="bi bi-geo-alt me-2"></i>
                <span><%= farm.location %></span>
              </div>
              
              <div class="farm-contact mb-3">
                <i class="bi bi-telephone me-2"></i>
                <span><%= farm.contact_phone %></span>
              </div>
              
              <div class="farm-metrics">
                <div class="metric-item">
                  <div class="metric-icon cows">
                    <i class="bi bi-emoji-smile"></i>
                  </div>
                  <div class="metric-content">
                    <div class="metric-value"><%= farm.total_cows %></div>
                    <div class="metric-label">Total Cows</div>
                  </div>
                </div>
                
                <div class="metric-item">
                  <div class="metric-icon active">
                    <i class="bi bi-heart-fill"></i>
                  </div>
                  <div class="metric-content">
                    <div class="metric-value"><%= farm.active_cows %></div>
                    <div class="metric-label">Active</div>
                  </div>
                </div>
                
                <div class="metric-item">
                  <div class="metric-icon production">
                    <i class="bi bi-droplet-fill"></i>
                  </div>
                  <div class="metric-content">
                    <div class="metric-value">
                      <%= number_with_precision(farm.today_production, precision: 1) %>L
                    </div>
                    <div class="metric-label">Today</div>
                  </div>
                </div>
              </div>
            </div>
            
            <div class="farm-card-footer">
              <div class="farm-actions">
                <%= link_to farm, class: "btn btn-sm btn-primary flex-grow-1 me-1" do %>
                  <i class="bi bi-eye me-1"></i>View
                <% end %>
                <%= link_to farm_cows_path(farm), class: "btn btn-sm btn-success flex-grow-1 me-1" do %>
                  <i class="bi bi-emoji-smile me-1"></i>Cows
                <% end %>
                <div class="btn-group">
                  <button class="btn btn-sm btn-outline-secondary dropdown-toggle" 
                          data-bs-toggle="dropdown">
                    <i class="bi bi-three-dots"></i>
                  </button>
                  <ul class="dropdown-menu dropdown-menu-end">
                    <li>
                      <%= link_to edit_farm_path(farm), class: "dropdown-item" do %>
                        <i class="bi bi-pencil me-2"></i>Edit Farm
                      <% end %>
                    </li>
                    <li>
                      <%= link_to farm_production_records_path(farm), class: "dropdown-item" do %>
                        <i class="bi bi-clipboard-data me-2"></i>Production Records
                      <% end %>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                      <%= link_to farm_path(farm), method: :delete, 
                                  confirm: "Are you sure you want to delete this farm?", 
                                  class: "dropdown-item text-danger" do %>
                        <i class="bi bi-trash me-2"></i>Delete Farm
                      <% end %>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- List View -->
  <div class="farms-container list-view" id="listViewContainer" style="display: none;">
    <div class="farms-list-card">
      <div class="list-header">
        <div class="row">
          <div class="col-md-3"><strong>Farm Details</strong></div>
          <div class="col-md-2 text-center"><strong>Location</strong></div>
          <div class="col-md-2 text-center"><strong>Cows</strong></div>
          <div class="col-md-2 text-center"><strong>Production</strong></div>
          <div class="col-md-3 text-center"><strong>Actions</strong></div>
        </div>
      </div>
      
      <div class="list-body" id="farmsList">
        <% @farms.each do |farm| %>
          <div class="farm-list-item" 
               data-name="<%= farm.name.downcase %>" 
               data-owner="<%= farm.owner.downcase %>" 
               data-location="<%= farm.location.downcase %>"
               data-cows="<%= farm.total_cows %>"
               data-production="<%= farm.today_production %>">
            <div class="row align-items-center">
              <div class="col-md-3">
                <div class="farm-list-info">
                  <div class="farm-list-icon">
                    <i class="bi bi-house-door-fill"></i>
                  </div>
                  <div class="farm-list-details">
                    <h6 class="farm-list-name"><%= farm.name %></h6>
                    <small class="farm-list-owner">
                      <i class="bi bi-person me-1"></i><%= farm.owner %>
                    </small>
                    <small class="farm-list-contact d-block">
                      <i class="bi bi-telephone me-1"></i><%= farm.contact_phone %>
                    </small>
                  </div>
                </div>
              </div>
              <div class="col-md-2 text-center">
                <span class="location-badge">
                  <i class="bi bi-geo-alt me-1"></i>
                  <%= farm.location %>
                </span>
              </div>
              <div class="col-md-2 text-center">
                <div class="cow-metrics">
                  <div class="metric-row">
                    <span class="metric-label">Total:</span>
                    <span class="metric-value"><%= farm.total_cows %></span>
                  </div>
                  <div class="metric-row">
                    <span class="metric-label">Active:</span>
                    <span class="metric-value text-success"><%= farm.active_cows %></span>
                  </div>
                </div>
              </div>
              <div class="col-md-2 text-center">
                <div class="production-display">
                  <div class="production-value">
                    <%= number_with_precision(farm.today_production, precision: 1) %>L
                  </div>
                  <div class="production-label">Today</div>
                </div>
              </div>
              <div class="col-md-3">
                <div class="farm-list-actions">
                  <%= link_to farm, class: "btn btn-sm btn-primary me-1" do %>
                    <i class="bi bi-eye"></i>
                  <% end %>
                  <%= link_to farm_cows_path(farm), class: "btn btn-sm btn-success me-1" do %>
                    <i class="bi bi-emoji-smile"></i>
                  <% end %>
                  <%= link_to edit_farm_path(farm), class: "btn btn-sm btn-outline-secondary me-1" do %>
                    <i class="bi bi-pencil"></i>
                  <% end %>
                  <%= link_to farm_path(farm), method: :delete, 
                              confirm: "Are you sure?", 
                              class: "btn btn-sm btn-outline-danger" do %>
                    <i class="bi bi-trash"></i>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
<% else %>
  <div class="empty-state">
    <div class="empty-state-content">
      <div class="empty-state-icon">
        <i class="bi bi-house-door"></i>
      </div>
      <h3 class="empty-state-title">No Farms Yet</h3>
      <p class="empty-state-subtitle">
        Start building your dairy empire by adding your first farm.
      </p>
      <div class="empty-state-actions">
        <%= link_to new_farm_path, class: "btn btn-success btn-lg" do %>
          <i class="bi bi-plus-circle me-2"></i>Add Your First Farm
        <% end %>
      </div>
      <div class="empty-state-suggestions mt-4">
        <h6>Getting started is easy:</h6>
        <ul class="list-unstyled">
          <li><i class="bi bi-check-circle me-2 text-success"></i>Add farm details and owner information</li>
          <li><i class="bi bi-check-circle me-2 text-success"></i>Register your cows with unique identifiers</li>
          <li><i class="bi bi-check-circle me-2 text-success"></i>Start tracking daily milk production</li>
        </ul>
      </div>
    </div>
  </div>
<% end %>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const farmSearch = document.getElementById('farmSearch');
  const farmSort = document.getElementById('farmSort');
  const gridViewBtn = document.getElementById('gridView');
  const listViewBtn = document.getElementById('listView');
  const gridContainer = document.getElementById('gridViewContainer');
  const listContainer = document.getElementById('listViewContainer');
  const farmsGrid = document.getElementById('farmsGrid');
  const farmsList = document.getElementById('farmsList');

  // View Toggle Functionality
  if (gridViewBtn && listViewBtn) {
    gridViewBtn.addEventListener('click', function() {
      gridViewBtn.classList.add('active');
      listViewBtn.classList.remove('active');
      gridContainer.style.display = 'block';
      listContainer.style.display = 'none';
      localStorage.setItem('farmsView', 'grid');
    });
    
    listViewBtn.addEventListener('click', function() {
      listViewBtn.classList.add('active');
      gridViewBtn.classList.remove('active');
      gridContainer.style.display = 'none';
      listContainer.style.display = 'block';
      localStorage.setItem('farmsView', 'list');
    });
    
    // Restore saved view preference
    const savedView = localStorage.getItem('farmsView');
    if (savedView === 'list') {
      listViewBtn.click();
    }
  }

  // Search Functionality
  if (farmSearch) {
    farmSearch.addEventListener('input', function() {
      const searchTerm = this.value.toLowerCase();
      filterAndSortFarms(searchTerm);
    });
  }

  // Sort Functionality
  if (farmSort) {
    farmSort.addEventListener('change', function() {
      const searchTerm = farmSearch ? farmSearch.value.toLowerCase() : '';
      filterAndSortFarms(searchTerm);
    });
  }

  function filterAndSortFarms(searchTerm) {
    const farmItems = document.querySelectorAll('.farm-item, .farm-list-item');
    const visibleItems = [];

    // Filter items
    farmItems.forEach(item => {
      const name = item.dataset.name || '';
      const owner = item.dataset.owner || '';
      const location = item.dataset.location || '';
      
      const matches = !searchTerm || 
        name.includes(searchTerm) || 
        owner.includes(searchTerm) || 
        location.includes(searchTerm);
      
      if (matches) {
        item.style.display = '';
        visibleItems.push(item);
      } else {
        item.style.display = 'none';
      }
    });

    // Sort visible items
    const sortBy = farmSort ? farmSort.value : 'name';
    visibleItems.sort((a, b) => {
      let aValue, bValue;
      
      switch(sortBy) {
        case 'name':
          aValue = a.dataset.name;
          bValue = b.dataset.name;
          break;
        case 'cows':
          aValue = parseInt(a.dataset.cows);
          bValue = parseInt(b.dataset.cows);
          break;
        case 'production':
          aValue = parseFloat(a.dataset.production);
          bValue = parseFloat(b.dataset.production);
          break;
        case 'location':
          aValue = a.dataset.location;
          bValue = b.dataset.location;
          break;
      }
      
      if (sortBy === 'cows' || sortBy === 'production') {
        return bValue - aValue; // Descending for numbers
      } else {
        return aValue.localeCompare(bValue); // Ascending for strings
      }
    });

    // Re-append sorted items
    const gridParent = farmsGrid;
    const listParent = farmsList;
    
    visibleItems.forEach(item => {
      if (item.classList.contains('farm-item')) {
        gridParent.appendChild(item);
      } else if (item.classList.contains('farm-list-item')) {
        listParent.appendChild(item);
      }
    });
  }

  // Card Hover Effects
  const farmCards = document.querySelectorAll('.farm-card');
  farmCards.forEach(card => {
    card.addEventListener('mouseenter', function() {
      this.style.transform = 'translateY(-8px)';
      this.style.boxShadow = '0 12px 30px rgba(0,0,0,0.15)';
    });
    
    card.addEventListener('mouseleave', function() {
      this.style.transform = 'translateY(0)';
      this.style.boxShadow = '0 4px 15px rgba(0,0,0,0.1)';
    });
  });

  // List Item Hover Effects
  const farmListItems = document.querySelectorAll('.farm-list-item');
  farmListItems.forEach(item => {
    item.addEventListener('mouseenter', function() {
      this.style.backgroundColor = 'var(--hover-bg)';
      this.style.transform = 'translateX(5px)';
    });
    
    item.addEventListener('mouseleave', function() {
      this.style.backgroundColor = '';
      this.style.transform = 'translateX(0)';
    });
  });

  // Stat Card Animations
  const statCards = document.querySelectorAll('.stat-card');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animationDelay = `${Array.from(statCards).indexOf(entry.target) * 0.1}s`;
        entry.target.classList.add('animate-in');
      }
    });
  });
  
  statCards.forEach(card => observer.observe(card));

  // Export functionality
  const exportBtn = document.getElementById('exportFarms');
  if (exportBtn) {
    exportBtn.addEventListener('click', function(e) {
      e.preventDefault();
      // This would typically make an AJAX request to export data
      console.log('Export farms data...');
      // For now, just show a message
      alert('Export functionality would be implemented here');
    });
  }

  // Search input focus effects
  if (farmSearch) {
    farmSearch.addEventListener('focus', function() {
      this.parentElement.classList.add('focused');
    });
    
    farmSearch.addEventListener('blur', function() {
      this.parentElement.classList.remove('focused');
    });
  }
});
</script>

<style>
/* Farms Management Enhanced Styles */
.farms-header {
  margin-bottom: 2rem;
}

.header-icon {
  width: 60px;
  height: 60px;
  background: var(--gradient-primary);
  border-radius: var(--border-radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  color: white;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.header-title {
  font-size: 2rem;
  font-weight: 700;
  background: var(--gradient-text);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.header-subtitle {
  color: var(--text-muted);
  font-size: 1rem;
}

.header-actions {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

/* Farm Statistics */
.farm-stats-container {
  margin-bottom: 2rem;
}

/* Filter Section */
.farms-filter-section {
  margin-bottom: 2rem;
}

.filter-card {
  background: var(--card-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--border-color);
  border-radius: var(--border-radius-lg);
  padding: 1.5rem;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.search-container {
  position: relative;
}

.search-icon {
  position: absolute;
  left: 1rem;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-muted);
  pointer-events: none;
  transition: color 0.2s ease;
}

.search-input {
  padding-left: 2.5rem;
  background: var(--input-bg);
  border: 1px solid var(--border-color);
  border-radius: var(--border-radius);
  transition: all 0.2s ease;
}

.search-container.focused .search-icon {
  color: var(--primary-color);
}

.search-input:focus {
  border-color: var(--primary-color);
  box-shadow: 0 0 0 0.25rem rgba(var(--primary-rgb), 0.1);
  background: white;
}

/* Farm Cards Grid */
.farms-container.grid-view .row {
  margin: 0 -0.5rem;
}

.farm-item {
  padding: 0 0.5rem;
  margin-bottom: 1rem;
}

.farm-card {
  background: var(--card-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--border-color);
  border-radius: var(--border-radius-lg);
  overflow: hidden;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
  height: 100%;
  display: flex;
  flex-direction: column;
}

.farm-card-header {
  background: var(--gradient-info);
  padding: 1.5rem;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.farm-header-content {
  display: flex;
  align-items: center;
  gap: 1rem;
  flex: 1;
}

.farm-icon {
  width: 50px;
  height: 50px;
  background: rgba(255,255,255,0.2);
  border-radius: var(--border-radius);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.25rem;
  color: white;
}

.farm-basic-info {
  flex: 1;
}

.farm-name {
  color: white;
  margin: 0 0 0.25rem 0;
  font-weight: 600;
  font-size: 1.125rem;
}

.farm-owner {
  color: rgba(255,255,255,0.9);
  margin: 0;
  font-size: 0.875rem;
  display: flex;
  align-items: center;
}

.farm-status {
  align-self: flex-start;
}

.status-badge {
  background: rgba(255,255,255,0.2);
  color: white;
  padding: 0.25rem 0.75rem;
  border-radius: 15px;
  font-size: 0.75rem;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 0.25rem;
}

.status-badge.active {
  background: rgba(34, 197, 94, 0.3);
}

.farm-card-body {
  padding: 1.5rem;
  flex: 1;
}

.farm-location,
.farm-contact {
  display: flex;
  align-items: center;
  color: var(--text-muted);
  font-size: 0.875rem;
}

.farm-metrics {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-top: 1.5rem;
}

.metric-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1rem;
  background: var(--muted-bg);
  border-radius: var(--border-radius);
  transition: all 0.2s ease;
}

.metric-item:hover {
  background: var(--hover-bg);
  transform: translateY(-2px);
}

.metric-icon {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
  color: white;
}

.metric-icon.cows {
  background: var(--gradient-primary);
}

.metric-icon.active {
  background: var(--gradient-success);
}

.metric-icon.production {
  background: var(--gradient-info);
}

.metric-content {
  flex: 1;
}

.metric-value {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--text-primary);
  line-height: 1;
}

.metric-label {
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-top: 0.25rem;
  font-weight: 500;
}

.farm-card-footer {
  padding: 1rem 1.5rem;
  background: var(--muted-bg);
  border-top: 1px solid var(--border-color);
}

.farm-actions {
  display: flex;
  gap: 0.5rem;
  align-items: center;
}

/* List View Styles */
.farms-list-card {
  background: var(--card-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--border-color);
  border-radius: var(--border-radius-lg);
  overflow: hidden;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.list-header {
  background: var(--gradient-primary);
  padding: 1rem 1.5rem;
  color: white;
  font-weight: 600;
}

.list-body {
  padding: 0;
}

.farm-list-item {
  padding: 1.5rem;
  border-bottom: 1px solid var(--border-color);
  transition: all 0.2s ease;
}

.farm-list-item:last-child {
  border-bottom: none;
}

.farm-list-info {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.farm-list-icon {
  width: 50px;
  height: 50px;
  background: var(--gradient-info);
  border-radius: var(--border-radius);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.25rem;
  color: white;
}

.farm-list-details {
  flex: 1;
}

.farm-list-name {
  margin: 0 0 0.25rem 0;
  font-weight: 600;
  color: var(--text-primary);
}

.farm-list-owner,
.farm-list-contact {
  color: var(--text-muted);
  font-size: 0.875rem;
  margin: 0;
}

.location-badge {
  background: var(--muted-bg);
  padding: 0.5rem 1rem;
  border-radius: 20px;
  font-size: 0.875rem;
  color: var(--text-primary);
  display: inline-flex;
  align-items: center;
}

.cow-metrics {
  text-align: center;
}

.metric-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 0.25rem;
}

.metric-row:last-child {
  margin-bottom: 0;
}

.metric-row .metric-label {
  font-size: 0.75rem;
  color: var(--text-muted);
  font-weight: 500;
}

.metric-row .metric-value {
  font-weight: 600;
  color: var(--text-primary);
}

.production-display {
  text-align: center;
}

.production-value {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--primary-color);
  line-height: 1;
}

.production-label {
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-top: 0.25rem;
}

.farm-list-actions {
  display: flex;
  gap: 0.25rem;
  justify-content: center;
}

/* Empty State */
.empty-state {
  text-align: center;
  padding: 4rem 2rem;
  background: var(--card-bg);
  border-radius: var(--border-radius-lg);
  border: 2px dashed var(--border-color);
  margin: 2rem 0;
}

.empty-state-icon {
  font-size: 5rem;
  color: var(--text-muted);
  margin-bottom: 1.5rem;
}

.empty-state-title {
  color: var(--text-primary);
  font-weight: 600;
  margin-bottom: 0.5rem;
  font-size: 1.75rem;
}

.empty-state-subtitle {
  color: var(--text-muted);
  margin-bottom: 2rem;
  font-size: 1.125rem;
}

.empty-state-actions {
  margin-bottom: 2rem;
}

.empty-state-suggestions {
  background: var(--muted-bg);
  padding: 1.5rem;
  border-radius: var(--border-radius);
  display: inline-block;
  text-align: left;
}

.empty-state-suggestions h6 {
  margin-bottom: 1rem;
  color: var(--text-primary);
  font-weight: 600;
}

.empty-state-suggestions li {
  margin-bottom: 0.5rem;
  color: var(--text-muted);
}

/* View Toggle Styles */
.view-options .btn-group {
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  border-radius: var(--border-radius);
}

.view-options .btn {
  border-color: var(--border-color);
  background: var(--card-bg);
  color: var(--text-muted);
  transition: all 0.2s ease;
}

.view-options .btn.active {
  background: var(--primary-color);
  color: white;
  border-color: var(--primary-color);
}

.view-options .btn:hover:not(.active) {
  background: var(--hover-bg);
  color: var(--primary-color);
}

/* Responsive Design */
@media (max-width: 768px) {
  .header-title {
    font-size: 1.5rem;
  }
  
  .header-icon {
    width: 50px;
    height: 50px;
    font-size: 1.25rem;
  }
  
  .farm-metrics {
    grid-template-columns: 1fr;
    gap: 0.75rem;
  }
  
  .farm-actions {
    flex-direction: column;
    gap: 0.5rem;
  }
  
  .farm-actions .btn {
    width: 100%;
  }
  
  .farms-filter-section .row {
    flex-direction: column;
  }
  
  .farms-filter-section .col-md-2,
  .farms-filter-section .col-md-4,
  .farms-filter-section .col-md-6 {
    margin-bottom: 1rem;
  }
  
  .farm-list-actions {
    flex-wrap: wrap;
  }
}

@media (max-width: 576px) {
  .farm-card-header {
    padding: 1rem;
  }
  
  .farm-card-body {
    padding: 1rem;
  }
  
  .farm-header-content {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
  }
  
  .farm-icon {
    width: 40px;
    height: 40px;
    font-size: 1rem;
  }
  
  .list-header,
  .farm-list-item {
    padding: 1rem;
  }
  
  .farm-list-info {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
  }
}
</style>
